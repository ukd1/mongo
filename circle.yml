dependencies:
  pre:
    - sudo aptitude install -y python-pymongo scons build-essential libboost-filesystem-dev libboost-program options-dev libboost-system-dev libboost-thread-dev ca-certificates curl git-core mercurial g++ dh-autoreconf pkg-config libgflags-dev
    - sudo apt-get install --only-upgrade gcc gcc++
    - sudo sh install_rocks.sh
test:
  override:
    - scons unittests
    - scons all